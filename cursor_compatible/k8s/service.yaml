apiVersion: v1
kind: Service
metadata:
  name: noderr-backend
  namespace: noderr-prod
spec:
  selector:
    app: noderr-backend
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP # Change to LoadBalancer for external access 